import 'package:flutter/material.dart';

class WrongPath extends StatelessWidget {
  const WrongPath({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrong Path'),
      ),
    );
  }
}
